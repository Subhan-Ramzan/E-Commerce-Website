export const metadata = {
    title: "Your Cart - New Fashion",
    description: "View and manage the items in your cart.",
  };
  
  export default function CartLayout({ children }) {
    return (
      <div>
        {children}
      </div>
    );
  }
  