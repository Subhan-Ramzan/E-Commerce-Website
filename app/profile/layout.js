export const metadata = {
    title: "Your Profile - New Fashion",
    description: "View and edit your profile information.",
  };
  
  export default function ProfileLayout({ children }) {
    return (
      <div>
        {children}
      </div>
    );
  }
  