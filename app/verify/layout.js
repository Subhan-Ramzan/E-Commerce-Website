export const metadata = {
    title: "Verify Your Account - New Fashion",
    description: "Verify your account to access your profile and orders.",
  };
  
  export default function VerifyLayout({ children }) {
    return (
      <div>
        {children}
      </div>
    );
  }
  